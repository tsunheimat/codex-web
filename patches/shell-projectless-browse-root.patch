--- a/.vite/build/src-DfDAEPNR.js
+++ b/.vite/build/src-DfDAEPNR.js
@@ -15422,7 +15422,8 @@
 }
 var pC = 5;
 function mC({ homeDirectory: e, path: t }) {
-  return t.join(e, `Documents`, `Codex`);
+  let n = process.env.CODEX_WEBUI_BROWSE_ROOT?.trim();
+  return n ? t.join(n, `Codex`) : t.join(e, `Documents`, `Codex`);
 }
 function hC({ date: e = new Date(), path: t, workspaceRoot: n }) {
   return t.join(n, _C(e));

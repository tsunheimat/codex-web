--- a/webview/assets/app-initial-BTphDPeq.js
+++ b/webview/assets/app-initial-BTphDPeq.js
@@ -583617,6 +583617,7 @@
   showUtilityBar: d,
   composerModeAvailability: f,
   placeholderText: p,
+  initialPrompt,
   composerLayoutMode: m,
   homeLeadingControl: h,
   hotkeyWindowHomeFooterControls: g,
@@ -584139,6 +584140,12 @@
       locationKey: lt.key,
       locationState: mt,
       setComments: qr,
+    }),
+    initialPromptEffect = (0, L$.useEffectEvent)(() => {
+      initialPrompt != null &&
+        initialPrompt !== `` &&
+        cn.getText() === `` &&
+        cn.setPromptText(initialPrompt);
     }),
     [Ri, zi] = q_(`composer_prefill`),
     Bi = (0, L$.useEffectEvent)(() => {
@@ -584154,6 +584161,9 @@
           zi(void 0)));
     });
   ((0, L$.useEffect)(() => {
+    initialPromptEffect();
+  }, [initialPrompt]),
+    (0, L$.useEffect)(() => {
     Bi();
   }, [Ri]),
     (0, L$.useEffect)(() => {}, [cn]));
@@ -586768,6 +586778,8 @@
   hideRunLocationDropdownOverride: v = !1,
   composerModeAvailability: y,
   placeholderText: b,
+  defaultText,
+  initialPrompt,
   composerLayoutMode: x = `multiline`,
   composerController: S,
   homeLeadingControl: C,
@@ -586901,8 +586913,8 @@
         S6o,
         {
           composerController: S,
-          defaultText: B,
-          defaultTextKind: H1r(B) ? `prompt` : `plain`,
+          defaultText: defaultText ?? B,
+          defaultTextKind: H1r(defaultText ?? B) ? `prompt` : `plain`,
           children: [
             null,
             w == null && n && r === `floating`
@@ -586943,6 +586955,7 @@
               hideRunLocationDropdownOverride: v,
               composerModeAvailability: y,
               placeholderText: b,
+              initialPrompt,
               composerLayoutMode: x,
               homeLeadingControl: C,
               hotkeyWindowHomeFooterControls: w,
@@ -592539,6 +592552,8 @@
     x = zw(),
     S = s8i(),
     C = Lw(),
+    [searchParams] = gVn(),
+    searchParamsPrompt = searchParams.get(`prompt`),
     w = C.state,
     T = r === `chat` && e != null,
     { data: E } = a2r(),
@@ -592820,7 +592835,9 @@
     nt = {
       browserConversationId: de,
       composerController: n,
+      defaultText: searchParamsPrompt ?? void 0,
       homeRunLocationRemoteProject: oe,
+      initialPrompt: searchParamsPrompt ?? void 0,
       onLocalConversationCreated: fe,
       placeholderText: Ge,
       selectedProject: V,
@@ -593136,7 +593153,9 @@
                               browserConversationId: de,
                               composerController: n,
                               composerLayoutMode: `auto-single-line`,
+                              defaultText: searchParamsPrompt ?? void 0,
                               homeRunLocationRemoteProject: oe,
+                              initialPrompt: searchParamsPrompt ?? void 0,
                               onLocalConversationCreated: fe,
                               showUtilityBar: o,
                               hideRunLocationDropdownOverride: !se,

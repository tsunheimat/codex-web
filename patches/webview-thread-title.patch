--- a/webview/assets/local-conversation-thread-BaIREoHa.js
+++ b/webview/assets/local-conversation-thread-BaIREoHa.js
@@ -493,6 +493,7 @@
   dt as $s,
   e1 as ec,
   e3 as tc,
+  e4 as codexHostedWindowTitleAtom,
   e9 as nc,
   eG as rc,
   eS as ic,
@@ -19786,6 +19787,7 @@
   let x = q(ko),
     S = W(ns, e) ?? !0,
     C = W(co, e),
+    codexHostedWindowTitle = W(codexHostedWindowTitleAtom, e),
     w = ad(`2138468235`).get(`enable_mcp_apps`, !1),
     { data: T } = W(ra, s, { enabled: w && e != null }),
     { visibleTurnEntries: E } = W(ii, {
@@ -19886,6 +19888,10 @@
   (0, zO.useEffect)(() => {
     o && ue();
   }, [ue, o]);
+  (0, zO.useEffect)(() => {
+    let t = codexHostedWindowTitle?.trim();
+    t && (document.title = `${t} | Codex`);
+  }, [codexHostedWindowTitle]);
   let de = K((e) => {
       ((V.current = e), B(e));
     }),

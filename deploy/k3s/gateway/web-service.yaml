apiVersion: v1
kind: Service
metadata:
  name: codex-web-desktop-ui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: codex-web-desktop-ui
  ports:
    - name: http
      port: 8214
      targetPort: http

apiVersion: v1
kind: Service
metadata:
  name: codex-web-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: codex-web-gateway
  ports:
    - name: http
      port: 8215
      targetPort: http
